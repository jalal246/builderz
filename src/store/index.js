import State from "./store";

import {
  extractBundleOpt,
  initOpts,
  extractAlias,
  extractEntries,
  extractName,
} from "./stateHandlers";

export {
  State,
  extractBundleOpt,
  initOpts,
  extractAlias,
  extractEntries,
  extractName,
};
